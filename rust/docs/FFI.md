# C FFI API

## Header

Generated by `cbindgen` from `turboquant-ffi/src/lib.rs`.

```c
#include "turboquant.h"
```

## Functions

### `tq_compress`

```c
tq_kv_block_t* tq_compress(const float* data, size_t len, uint8_t bits,
                           size_t block_size);
```

Compress a float array into a packed KV block.

### `tq_decompress`

```c
int tq_decompress(const tq_kv_block_t* block, float* out, size_t out_len);
```

Decompress a packed KV block back to floats.

### `tq_attention_forward`

```c
int tq_attention_forward(const float* q, const tq_kv_block_t* k_cache,
                         const tq_kv_block_t* v_cache, float* out);
```

Compute attention using compressed K/V caches.

### `tq_free_block`

```c
void tq_free_block(tq_kv_block_t* block);
```

Free a compressed block.

## Example

See `examples/c/main.c` for a complete example.

//! GGUF file writer.

/// Placeholder for GGUF writing functionality.
pub struct GgufWriter;

impl GgufWriter {
    /// Create a new GGUF writer.
    #[must_use]
    pub fn new() -> Self {
        Self
    }
}

impl Default for GgufWriter {
    fn default() -> Self {
        Self::new()
    }
}
